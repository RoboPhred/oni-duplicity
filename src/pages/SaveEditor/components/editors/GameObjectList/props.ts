export interface GameObjectListProps {
  /**
   * Specifies if the target value is an array of
   * game objects.
   * If false or unset, a GameObjectGroup is assumed.
   */
  isFlatList?: boolean;
}
